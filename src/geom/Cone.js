VERB.geom.Cone = function(axis, base, top, radius) {

	this.axis = axis;
	this.base = base;
	this.top = top;
	this.radius = radius;

	this.as_nurbs_surface = function() {



    // construct nurbs surface
  };

};
